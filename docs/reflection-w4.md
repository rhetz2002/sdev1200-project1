The main thing I'm proud of for v1 is the spawning/hitboxes as i wasn't sure how I would be doing that initially. I knew I'd be making different instances of the same sprite, but I wasn't sure if I would be able to kill a single instance without messing up the rest of the sprite instances. thankfully i was able to do that. 

The biggest technical problems I was having was with drawing and wrapping my head around it all. Thankfully I was able to figure it all out. Another technical obstacle I would like to throw out there was the gui, i.e. the hp bar, it just turned out i was over complicating it all and I could just render the hp bar by putting a single draw rect line to reference the player hp as part of the width and update it whenever the main loop hits it again. 

the biggest thing i want to fix is organization as it is currently a hot mess. most of the logic is in the main file and it could be offloaded to secondary files, not to mention that there is little to no organization in it currently. but other things I'd like to fix is true random spawning as its currently pulling for a static dictionary, as well as fixing the player from not going off the screen, I would also like to add enemy health as well.  